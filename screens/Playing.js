import * as React from "react";
import Layout from "../components/Layout";
import { Text } from "@ui-kitten/components";

const Playing = (props) => {
  return (
    <Layout>
      <Text category="h1">Playing</Text>
    </Layout>
  );
};

export default Playing;
